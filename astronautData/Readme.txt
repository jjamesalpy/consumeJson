
/************************************************/
The application runs on the default port.

I have added spring boot accutator for health checks. Where all the end points are enabled.

While fetchin an astronaut if a record is not found the error messae is not handled in the response.

This application mainly has 3 class files
1)AstronautService.java - > Where the jSon parsin loic is written
2)AstonautModel.java -> The pojo class
3)GetAstronautDataController.java -> the rest API controller.

/************************************************/


